<main>
  <h1>Categories</h1>

  <ul>
     <% @categories.each do |category| %>
      <li><%= link_to "#{category.category}", category_products_path(category[:id]) %></li>
    <% end %>
  </ul>

  <%= link_to "Add a new category", new_category_path, class: "btn btn-primary" %>

</main>